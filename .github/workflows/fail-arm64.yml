name: bazel, fail (bento), arm64

on:
  workflow_dispatch:

jobs:

  build:
    runs-on:
    - self-hosted
    - "os=linux"
    - "arch=arm64"
    - "engflow-cluster=local"
    - "engflow-bento-name=mybento-arm64"
    - "engflow-bento-allow-save=true"

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 1

    - name: Run bazel
      run: |
        set ; false
